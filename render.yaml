services:
  - type: web
    name: carepilot-backend
    runtime: docker
    dockerContext: .
    dockerfilePath: backend/Dockerfile
    autoDeployTrigger: commit
    healthCheckPath: /docs
    envVars:
      - key: ALLOW_ANON
        value: "true"
