services:
  - type: web
    name: carepilot-backend
    env: docker
    rootDir: .
    dockerfilePath: backend/Dockerfile
    autoDeploy: true
    plan: starter
    healthCheckPath: /docs
    envVars:
      - key: ALLOW_ANON
        value: "true"

  - type: web
    name: carepilot-carebase
    env: docker
    rootDir: .
    dockerfilePath: carebase_server/Dockerfile
    autoDeploy: true
    plan: starter
    envVars:
      - key: PORT
        value: "3100"

  - type: web
    name: carepilot-frontend
    env: docker
    rootDir: .
    dockerfilePath: frontend/Dockerfile
    autoDeploy: true
    plan: starter
    envVars:
      - key: BACKEND_URL
        value: https://carepilot-backend.onrender.com
*** End Patch"}`
