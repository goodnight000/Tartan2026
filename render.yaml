services:
  - type: web
    name: carepilot-backend
    runtime: docker
    dockerContext: .
    dockerfilePath: backend/Dockerfile
    autoDeploy: true
    healthCheckPath: /docs
    envVars:
      - key: ALLOW_ANON
        value: "true"
